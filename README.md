# movie-clone

## Getting Started

Run:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
```

## Docker

Build image:

```bash
docker-compose build
```

Run container:

```bash
docker-compose up
```

Run container: (localhost:3000)

```bash
docker-compose down
```
